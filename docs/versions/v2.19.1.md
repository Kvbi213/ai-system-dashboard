# Wydanie v2.19.1 — Dedykowana Zakładka OMNIDAEMON, Dwukierunkowy Czat Smartfonowy Pushbullet i Ochrona Przed Pętlą Echa

**Data wydania:** 2026-09-16  
**Typ wydania:** PATCH (rozszerzenie interfejsu o tryb OMNIDAEMON, obsługa zapytań ze smartfona przez Pushbullet z obowiązkową odpowiedzią zwrotną push, synchronizacja kolekcji chmurowej i ochrona pętli echa)  
**Status:** Wdrożone i zweryfikowane (114/114 testów PASS)  

---

## 1. Zakres Zmian

### `[+]` Nowe Funkcjonalności i Ulepszenia
- **Dedykowana Zakładka OMNIDAEMON w Terminalu (`modules/components/Terminal.jsx`):**
  - Dodano 3. przycisk trybu w nagłówku terminala obok `WORKER` i `MENTOR`: `<Bot /> OMNIDAEMON`.
  - Wizualne wsparcie dla trybu `daemon`: dedykowana ikona bota, etykieta nagłówka `OMNIDAEMON`, specyficzne komunikaty nasłuchu mowy (`OMNIDAEMON // SŁUCHA...`, `OMNIDAEMON // ANALIZUJE...`, `OMNIDAEMON // ODPOWIADA...`) oraz loader przetwarzania.
  - Wybór i renderowanie wiadomości ze stanu `daemonMessages`.
- **Zarządzanie Stanem i Chmurą OMNIDAEMON (`modules/context/ChatContext.jsx`):**
  - Rozszerzono `ChatContext` o stan `daemonMessages` oraz `setDaemonMessages`.
  - Pamięć lokalna z kluczem `system_daemon_history` oraz znacznikiem czyszczenia `system_chat_cleared_daemon`.
  - Subskrypcja Firestore `chat_history` z filtrowaniem i scalaniem wiadomości dla `chatMode: 'daemon'`.
  - Obsługa poleceń systemowych:
    - `/clear` – czyści historię OMNIDAEMON i usuwa dokumenty z Firestore dla `chatMode === 'daemon'`.
    - `/mode daemon` – przełącza na tryb autonomiczny OMNIDAEMON z potwierdzeniem systemowym.
    - `/help` – zaktualizowano listę trybów do `[worker|mentor|daemon]`.
    - `/purge` – czyści pamięć i chmurę dla wszystkich trzech trybów naraz.
    - `/export` – eksportuje historię aktywnego trybu (w tym OMNIDAEMON).
  - Bezpośrednia dyspozycja zapytań z zakładki OMNIDAEMON do silnika AI z utrwaleniem w Firestore pod `chatMode: 'daemon'`.
- **Dwukierunkowy Czat Smartfonowy przez Pushbullet (`modules/pushbullet.js` & `api/pushbullet-webhook.js`):**
  - **Odbiór zapytań ogólnych ze smartfona:** Dowolna notatka/wiadomość z telefonu niebędąca zapytaniem o status czy zatrzymaniem trafia do funkcji `handleMobileChatQuery(content)`.
  - Zapis wiadomości użytkownika do Firestore `chat_history` (`chatMode: 'daemon'`, `source: 'pushbullet_mobile'`).
  - Przetworzenie intencji przez silnik AI (`processUserIntent` lub Groq `openai/gpt-oss-120b`).
  - Zapis odpowiedzi AI do Firestore `chat_history` (`chatMode: 'daemon'`, `source: 'omni_daemon'`).
  - **BEZWZGLĘDNY WYMÓG SPEŁNIONY:** Automatyczne odesłanie kompletnej odpowiedzi AI na smartfon operatora przez Pushbullet API (`sendPushNotification('OmniDash AI 🤖', aiText)`).
  - Emisja zdarzenia SSE `chat_message_received` do wszystkich otwartych pulpitów sterowniczych.
- **Ochrona Przed Pętlą Echa Powiadomień (`isOwnSystemNotification`):**
  - Weryfikacja nagłówków i treści przychodzących powiadomień w poszukiwaniu znaczników systemowych (`OmniDash`, `OmniAgent`, `OmniDaemon`, `Auto-Finanse`).
  - Automatyczne ignorowanie powiadomień wyemitowanych przez sam system zapobiegające zapętleniu konwersacji.
  - Zapisywanie identyfikatora wysłanego pusha (`lastProcessedPushId = data.iden`) w celach natychmiastowej deduplikacji zdarzeń `tickle`.
- **Dedykowany Prompt Operacyjny OmniDaemon (`modules/services/clientAiDispatcher.js`):**
  - Specjalizacja systemowa dla OMNIDAEMON: agent 24/7 z dostępem do internetu, bazy danych, zadań To-Do, planu lekcji, budżetu 50/30/20, kalendarza i Pushbullet API.

### `[*]` Poprawki i Optymalizacje
- **Eliminacja błędu składniowego w dyspozytorze klienta:**
  - Naprawiono domknięcie warunku trójargumentowego promptu systemowego w `modules/services/clientAiDispatcher.js`.
- **Bezpieczeństwo Groq w środowiskach hybrydowych/testowych:**
  - Zapewniono opcję `dangerouslyAllowBrowser: true` w `modules/agent.js`, co eliminuje wyjątki biblioteczne Groq w środowisku JSDOM podczas wykonywania testów jednostkowych.

### `[+]` Nowe Testy Jednostkowe (`tests/autonomous_agent.test.js`)
- Dodano 4 nowe testy jednostkowe:
  - Weryfikacja filtracji pętli echa (`isOwnSystemNotification`),
  - Obsługa zapytań ze smartfona i generowanie struktur z `chatMode: 'daemon'` oraz `source: 'pushbullet_mobile'`,
  - Weryfikacja obowiązkowego dispatchu odpowiedzi push zwrotnej na telefon,
  - Obsługa wartości brzegowych (puste i nieważne zapytania).
- Łączna liczba testów w systemie: **114/114 PASS** (9 zestawów testowych).

---

## 2. Checklista Integralności

| Weryfikacja | Rezultat |
| :--- | :--- |
| `npx vitest run` | **114/114 PASS** (9/9 zestawów testowych) |
| `npm run build` | **Zbudowano pomyślnie w 10.03s** |
| `scripts/sync_bez_firebase.js` | **100% synchronizacji lustrzanej** |
| `Git Working Tree` | **Przygotowano do wdrożenia na gałęzi main** |
