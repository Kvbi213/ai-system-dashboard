# Antigravity — OmniDash v2.18.8
**Data wdrożenia:** 2026-09-15  
**Autor:** Antigravity System Engineer  
**Typ zmiany:** MINOR / PATCH  

---

## [+] Nowe funkcjonalności
- **Direct Client Pushbullet Service (`modules/services/pushbulletService.js`):**
  - Bezpośrednia obsługa API Pushbullet (`https://api.pushbullet.com/v2/pushes`) z przeglądarki z nagłówkiem `Access-Token` dzięki pełnemu CORS.
  - Funkcja `sendPushNotificationClient(title, body)` z rygorystyczną walidacją `response.ok`, autoryzacji i precyzyjnymi toastami systemowymi (czerwony przy błędzie, niebieski/zielony przy sukcesie).
  - Funkcja `testPushbulletConnection(token)` do natychmiastowej weryfikacji konta i wysyłki testowego powiadomienia.
- **Panel Konfiguracyjny Pushbullet w Ustawieniach (`SettingsPage.jsx`):**
  - Nowa karta w zakładce `Bazy & Bezpieczeństwo` z edycją tokenu `Access-Token`, wskaźnikiem stanu połączenia i przyciskiem testu na żywo na smartfon.
- **Konfiguracja Środowiskowa:**
  - Wprowadzono zmienną `VITE_PUSHBULLET_API_KEY` do pliku `.env` i `.env.example`.

## [*] Modyfikacje i optymalizacje
- **Aktualizacja Planu Lekcji 2TI (od 14.09.2026 - Grupa 1):**
  - Przebudowano harmonogram do 38 precyzyjnych lekcji Grupy 1 na podstawie nowego arkusza szkolnego.
  - Dodano lekcję religii w poniedziałek o 8:00 (sala 38).
  - Zaktualizowano sale lekcyjne (Systemy operacyjne do sali 1.16, Matematyka do sali 26, Chemia w piątek do sali 19).
  - Przeniesiono Biznes i Zarządzanie na środę na godzinę 10:40 (sala 0.2).
  - Zsynchronizowano bazy: wyczyszczono 40 starych dokumentów z Cloud Firestore (`void-potato-7721`) i wgrano 38 nowych; zaktualizowano SQLite `data/tasks.sqlite` oraz `INITIAL_FIRESTORE_DATA.timetable` w `cloudSync.js`.
- **Eliminacja Silent Fail w AI Dispatcher (`clientAiDispatcher.js`):**
  - Usunięto cichy blok `fetch(pushEndpoint).catch(() => {})`, który tłumił błędy i pokazywał fałszywy sukces.
  - Asystent rzetelnie informuje o wysyłce lub przyczynie błędu w przypadku braku klucza.

## [!] Poprawki bezpieczeństwa i integralności
- Zabezpieczono dostęp do `localStorage` w `pushbulletService.js` przed środowiskami pozaprzeglądarkowymi (Node.js/JSDOM).
- Zapewniono 100% anonimizację poświadczeń (Ghost Operator) — pobieranie wyłącznie z `.env` / `localStorage`.

---

## Checklista Integralności
- [x] Składnia i kompilacja (`npm run build`): 0 BŁĘDÓW (czas: 23.01s).
- [x] Testy automatyczne (`npx vitest run`): 76/76 testów ZALICZONYCH (8 plików testowych).
- [x] Linter (`npm run lint`): 0 BŁĘDÓW (43 ostrzeżenia no-empty).
- [x] Synchronizacja repozytorium bez firebase (`scripts/sync_bez_firebase.js`): 100% spójności.
- [x] Zapis bazy Cloud Firestore (`void-potato-7721`): 38 dokumentów zsynchronizowanych.
- [x] Zapis bazy SQLite (`data/tasks.sqlite`): 38 rekordów zaktualizowanych.
- [x] Dokumentacja błędów (`docs/errors/ERROR_DIFF_2026-09-15_pushbullet_silent_fail_and_timetable_2ti.md`): ZAMKNIĘTA.
- [x] Spójność ARCHITECTURE.md i HISTORY.md: WYKONANA.

**Status: ZGODNY Z PROTOKOŁEM ANTIGRAVITY (NANO v3.2)**
