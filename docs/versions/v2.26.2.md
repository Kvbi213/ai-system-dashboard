# RAPORT WYDANIA: v2.26.2
Data wydania: 2026-09-24  
Typ: PATCH  
Status: Zweryfikowane produkcyjnie (190/190 testów PASS, Vite build SUCCESS)  

---

## 1. ZAKRES OPERACYJNY
Instalacja, autoryzacja i pełna konfiguracja Google Cloud SDK (`gcloud` CLI) na maszynie roboczej, aktywacja wymaganych interfejsów API w projekcie GCP `omnidash-509607` oraz spięcie subskrypcji Cloud Pub/Sub z webhookiem budżetowym OmniDash:
1. **Google Cloud SDK v586.0.0:** Cicha instalacja pakietu `Google.CloudSDK` przez menedżer Winget w ścieżce `%LOCALAPPDATA%\Google\Cloud SDK\google-cloud-sdk\bin` oraz rejestracja w systemowej zmiennej `PATH`.
2. **Autoryzacja Konta Operatora:** Zrealizowano autoryzację OAuth2 `gcloud auth login` dla konta `<OPERATOR_ACCOUNT>` i powiązano z projektem docelowym `omnidash-509607`.
3. **Aktywacja Interfejsów API Google Cloud:**
   - `texttospeech.googleapis.com` (Cloud Text-to-Speech API)
   - `directions-backend.googleapis.com` (Directions API)
   - `geocoding-backend.googleapis.com` (Geocoding API)
   - `billingbudgets.googleapis.com` (Cloud Billing Budget API)
   - `maps-backend.googleapis.com` (Maps JavaScript API – potwierdzone aktywne)
   - `pubsub.googleapis.com` (Cloud Pub/Sub API – potwierdzone aktywne)
4. **Cloud Pub/Sub Push Subscription:** Utworzono subskrypcję `projects/omnidash-509607/subscriptions/omni-budget-push` podpiętą pod temat `budget-auto-stop` przekazującą powiadomienia budżetowe do `https://void-potato-7721.web.app/api/gcp/budget-webhook`.
5. **Obsługa Tematu w Kodzie:** Wprowadzono zmienną `GCP_PUBSUB_TOPIC` w `.env`, zaktualizowano `gcpBudgetService.js` oraz test `tests/gcp_budget.test.js`.

---

## 2. INFRASTRUCTURE MAPPING
```
┌─────────────────────────────────────────────────────────────┐
│             GOOGLE CLOUD PLATFORM (omnidash-509607)         │
├─────────────────────────────────────────────────────────────┤
│ • Billing Account: 01492A-AB547B-8B9595 (Budżet 1.00 PLN)   │
│ • Progi alertowe:  50%, 90%, 100%                           │
│ • Temat Pub/Sub:   budget-auto-stop                         │
│                                                             │
│   ├── Subskrypcja 1 (Cloud Run):                            │
│   │   cr-service-europe-west1-stop-billing-on-budget        │
│   │   -> https://stop-billing-on-budget-*.run.app           │
│   │                                                         │
│   └── Subskrypcja 2 (OmniDash Webhook):                     │
│       omni-budget-push                                      │
│       -> https://void-potato-7721.web.app/api/gcp/budget-   │
│          webhook                                            │
├─────────────────────────────────────────────────────────────┤
│ AKTYWNE API:                                                │
│ • Cloud Text-to-Speech (texttospeech.googleapis.com)        │
│ • Directions API (directions-backend.googleapis.com)        │
│ • Geocoding API (geocoding-backend.googleapis.com)         │
│ • Cloud Billing Budget API (billingbudgets.googleapis.com)  │
│ • Maps JavaScript API (maps-backend.googleapis.com)         │
│ • Cloud Pub/Sub API (pubsub.googleapis.com)                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. CHECKLISTA INTEGRALNOŚCI
| Komponent | Konfiguracja | Rezultat |
|---|---|---|
| Pakiet `gcloud` CLI | Google Cloud SDK v586.0.0 w PATH | [+] ZALICZONE |
| Autoryzacja `gcloud` | Konto operatora zalogowane (`gcloud auth list`) | [+] ZALICZONE |
| Aktywny projekt `gcloud` | `omnidash-509607` (`gcloud config list`) | [+] ZALICZONE |
| Włączenie API Text-to-Speech | `texttospeech.googleapis.com` ENABLED | [+] ZALICZONE |
| Włączenie API Directions & Geocoding | `directions-backend`, `geocoding-backend` ENABLED | [+] ZALICZONE |
| Włączenie API Billing Budgets | `billingbudgets.googleapis.com` ENABLED | [+] ZALICZONE |
| Subskrypcja Push Pub/Sub | `omni-budget-push` na temacie `budget-auto-stop` | [+] ZALICZONE |
| Testy jednostkowe Vitest (14 plików) | 190/190 PASS | [+] 100% ZALICZONE |
| Kompilacja produkcyjna `npm run build` | Vite build clean | [+] SUCCESS (0 błędów) |
