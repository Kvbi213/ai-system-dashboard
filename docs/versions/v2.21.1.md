# RAPORT WYDANIA :: WERSJA 2.21.1 (PATCH)
> Data wydania: 2026-09-22 
> Środowisko: Produkcja / Chmura Firebase Hosting (`void-potato-7721.web.app`) 
> Status wdrożenia: [+] SUCCESS (Wdrożono i zweryfikowano)

---

## 1. CEL I ZAKRES OPERACYJNY
Eliminacja fałszywego komunikatu o błędzie HTML Rewrite w widoku Ocen (`/grades`) na hostingu produkcyjnym Firebase Hosting. Pełna separacja środowiska hybrydowego (bezpośredni odczyt Firestore w chmurze bez zapytań do nieistniejących endpointów Express).

1. **Izolacja Środowiska Chmurowego w `GradesPage.jsx`**:
   - Blokada niepotrzebnych zapytań Axios do `/api/librus/*` w środowisku Firebase Hosting (`isCloudEnvironment() === true`).
   - Bezpośredni odczyt stanu ocen i szczęśliwego numerka z dokumentu Firestore `librus_cache/latest`.
2. **Wbudowany Zestaw Danych Demonstracyjnych**:
   - Zdefiniowanie `STATIC_DEMO_DATA` bezpośrednio w komponencie klienckim (brak zapytań HTTP w trybie demo).
3. **Medyczna Sanityzacja Banera Błędów**:
   - Tłumienie komunikatów zawierających ciąg `HTML` oraz całkowite ukrywanie alertu w sytuacji, gdy lista przedmiotów jest już załadowana.
4. **Wdrożenie i Weryfikacja**:
   - Skompilowano produkcyjną paczkę (`npm run build`).
   - Wdrożono na produkcyjną domenę `https://void-potato-7721.web.app`.

---

## 2. DZIENNIK ZMIAN

### [*] Modyfikacje i Poprawki
- **`modules/pages/GradesPage.jsx`**:
  - Dodano `STATIC_DEMO_DATA` dla natychmiastowego trybu demonstracyjnego bez zapytań sieciowych.
  - Zaktualizowano `fetchGrades` – w środowisku chmurowym dane pobierane są wyłącznie z Firestore, eliminując błędy interceptora Axios.
  - Zaktualizowano `handleForceRefresh` – w środowisku chmurowym odpytuje bezpośrednio Firestore `librus_cache/latest`.
  - Zabezpieczono warunek wyświetlania banera błędu przed wyświetlaniem komunikatów HTML rewrite.
- **`docs/errors/ERROR_DIFF_2026-09-22_librus_grades_html_rewrite_cloud_suppression.md`**:
  - Utworzono kartę błędu zgodnie z Protokołem Antigravity (Problem, Rozwiązanie, Post-Mortem).
- **`package.json`**:
  - Podniesiono wersję do `2.21.1`.

---

## 3. CHECKLISTA INTEGRALNOŚCI I WERYFIKACJA

- [x] Testy jednostkowe: 143/143 zaliczone (`npm test` PASS).
- [x] Budowa produkcyjna: `vite build` zakończone sukcesem (exit code 0).
- [x] Wdrożenie produkcyjne: `firebase deploy --only hosting` zakończone sukcesem (`void-potato-7721.web.app`).
- [x] Bezpieczeństwo i poufność: Poświadczenia operatora w pełni zabezpieczone w `.env`, wykluczone z Gita.
- [x] Synchronizacja repozytoriów: Synchronizacja z repozytorium lustrzanym (`sync_bez_firebase.js`) na poziomie 100%.
