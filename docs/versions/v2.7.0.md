# Antigravity — OmniDash v 2.7.0
**Data wdrożenia:** 2026-09-07  
**Autor:** Antigravity Operational Protocol  
**Typ zmiany:** MINOR  

---

## [+] Zaimplementowane funkcjonalności
- **Personalizacja Paska Nawigacji (`system_visible_nav`)**:
  - Dedykowana podsekcja „Nawigacja & Zakładki” w `SettingsPage.jsx`.
  - Niezależne przełączniki widoczności dla 10 modułów systemu (`/`, `/chat`, `/timetable`, `/memory`, `/osint`, `/calendar`, `/finances`, `/workouts`, `/widgets`, `/server`).
  - Trwałe zabezpieczenie zakładki `Ustawienia` (`/settings`), która jest zawsze widoczna i zablokowana przed wyłączeniem.
  - Profile szybkiego wyboru: „Pokaż Wszystkie”, „Profil Minimalistyczny” (Pulpit, AI Chat, Szkoła, Finanse) oraz „Przywróć Domyślne”.
  - Natychmiastowe rozgłaszanie zdarzenia `visibleNavChanged` i live filtrowanie w `Sidebar.jsx`.

- **Ujednolicenie Wymiarów Widżetów (`WidgetsPage.jsx` & `SystemMonitor.jsx`)**:
  - Wdrożono regułę siatki CSS `auto-rows-[360px]` z kontenerami kart `h-[360px] flex flex-col`.
  - Skorygowano `SystemMonitor.jsx` z niestabilnego `h-fit` na `h-full flex flex-col`.
  - Wszystkie 8 widżetów (System Monitor, Network Monitor, Crypto Tracker, Scratchpad, Token Tracker, Model Status, Prompt Vault, Agent Queue) ma identyczną wysokość 360px i zachowuje wewnętrzne przewijanie zawartości bez rozpychania siatki.

- **Kolekcja Gotowych Motywów Wizualnych (Theme Flavors & Presets)**:
  - 🌌 **Dark Cyber (Domyślny)**: Głęboki grafit, neonowa zieleń sci-fi.
  - 📟 **Retro Amber CRT**: Ciepły kineskopowy bursztyn lat 80. (`#140E05` / `#FFB000`).
  - 🏁 **Monochrome Slate**: Czysta czerń, grafit i biel (`#0A0A0C` / `#F5F5F5`).
  - 🟢 **Matrix Terminal**: Hakerska zielona konsola (`#020B04` / `#00FF41`).
  - 🌆 **Synthwave 80s**: Neonowa magenta i fiolet cyberpunku (`#120824` / `#FF0080`).
  - ❄️ **Nordic Frost**: Krystaliczny chłodny błękit i arktyczny granat (`#0A131F` / `#38BDF8`).
  - 📄 **Paper Light**: Jasny tryb produktywny o wysokim kontraście (`#F4F4F5` / `#FFFFFF`).
  - Interaktywne karty podglądu z paletami próbek kolorów (tło, panele, akcent) i aktywacją jednym kliknięciem.

- **Zaawansowane Modyfikatory Interfejsu & AI**:
  - Przełącznik Glassmorphism (rozmycie tła / blur).
  - Przełącznik animacji interfejsu (płynne przejścia vs styl surowego terminala).
  - Tryb kompaktowy UI (zmniejszone marginesy i wysoka gęstość informacji).
  - Wybór domyślnego modelu AI (`openai/gpt-oss-120b`, `llama-3.3-70b-versatile`, `mixtral-8x7b-32768`, `deepseek-r1-distill-llama-70b`).
  - Kopia zapasowa konfiguracji: eksport do formatu JSON (`omnidash-config.json`) oraz import z pliku.

## [*] Modyfikacje istniejących komponentów
- `Sidebar.jsx`: nasłuchiwanie zdarzenia `visibleNavChanged` i dynamiczne renderowanie `displayedNavItems`.
- `core.client.jsx`: inicjalizacja modyfikatorów UI (`compact-mode`, `no-glass`, `no-animations`) oraz poprawna inicjalizacja akcentów z localStorage.
- `assets/styles/index.css`: dodanie klas `.no-animations`, `.no-glass`, `.compact-mode`.
- `scripts/sync_bez_firebase.js`: rozszerzenie o synchronizację wszystkich nowych komponentów do repozytorium lustrzanego.

## Audyt Integralności
- [x] Integralność ścieżek: ZGODNA
- [x] Logika rygoru protokołu: ZGODNA
- [x] Czystość outputu: ZGODNA
- [x] Weryfikacja kompilacji (`npm run build`): 0 BŁĘDÓW W OBU REPOZYTORIACH
- [x] Wdrożenie produkcyjne Firebase Hosting: WYKONANE (`https://void-potato-7721.web.app`)
- [x] Wdrożenie produkcyjne Vercel: WYKONANE (`https://ai-system-dashboard.vercel.app`)
- [x] Aktualizacja HISTORY.md: WYKONANA

**Status: ZGODNY Z PROTOKOŁEM ANTIGRAVITY**
